#include <stdio.h>
#include "shell.h"

// Function that displays the first shell prompt
void print_prompt1(void) {
    // Print "$ " to the console
    printf("$ ");
}

// Function that displays the second shell prompt
void print_prompt2(void) {
    // Print "> " to the console
    printf("> ");
}
